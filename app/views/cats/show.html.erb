<h1><%= @cat.name %></h1>

<table>
  <tr>
    <td>Color</td> <td> <%= @cat.color %> </td>
  </tr>
  <tr>
    <td>Birthday</td> <td> <%= @cat.birth_date %> </td>
  </tr>
  <tr>
    <td>Age</td> <td> <%= @cat.age %> </td>
  </tr>
  <tr>
    <td>Gender</td> <td> <%= @cat.sex %> </td>
  </tr>
  <tr>
    <td>Description</td> <td> <%= @cat.description %> </td>
  </tr>
</table>

<br>
<table>
  <tr>
    <th>Start Date</th> <th>End Date</th>
  </tr>
  <% @cat.ordered_rental_requests.each do |rental| %>
  <tr>
    <td><%= rental.start_date %></td> <td><%= rental.end_date %></td>

    <% if rental.status == 'PENDING' %>
    <td>
      <form action="<%= approve_cat_rental_request_url %>", method="POST">
        <input type="hidden" name="_method" value="PATCH">
        <button>Approve</button>
      </form>
    </td>
    <td>
      <form action="<%= deny_cat_rental_request_url %>", method="POST">
        <input type="hidden" name="_method" value="PATCH">
        <button>Deny</button>
      </form>
    </td>
       <!-- <button_to >Deny</button> -->

    <% end %>
  </tr>
  <% end %>
</table>
