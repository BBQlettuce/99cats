<h1>All Cats</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <!-- <a href="/cats/<%= cat.id %>"> <%= cat.name %> </a> -->
       <a href="<%= cat_url(cat) %>"><%= cat.name %>
    </li>
  <% end %>
</ul>

<br>
<% if logged_in? %>
<a href="<%= new_cat_url %>">Register a new cat!~~~ </a>
<br>
<a href="<%= new_cat_rental_request_url %>">RENT-A-CAT</a>

<% end %>
