<h1>All Cats</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <!-- <a href="/cats/<%= cat.id %>"> <%= cat.name %> </a> -->
       <a href="<%= cat_url(cat) %>"><%= cat.name %>
    </li>
  <% end %>
</ul>
